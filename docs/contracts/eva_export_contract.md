# EVA Export Contract

Current version: `docs/contracts/eva_export_contract_v1.md`.

EVA-ready means local validation and manual review, not direct Sentry submission. EVA JSON is generated from canonical parser/review data after export gates pass.

## Open Items

- Confirm final EVA JSON field names against live EVA import behaviour.
- Add golden tests for all 26 parser presets.
- Decide the final machine-readable audit metadata shape for manual corrections.

When to read this: read when editing templates, CSS, or brand assets.

# Brand

Brand red: `#C80A32` for report table headings and section rules.

The colour is sampled from the rendered `referencefiles/example_report.pdf` table/header accents. The logo itself contains a brighter red gradient from `referencefiles/logo.pdf`.

Logo assets:

- `assets/brand/logo.svg`
- `assets/brand/logo.png`

Footer string:

`Collision Engineers Ltd | www.CollisionEngineers.co.uk | engineers@collisionengineers.co.uk`

Font family:

Use a locked sans-serif stack: `Arial, Helvetica, sans-serif`.
